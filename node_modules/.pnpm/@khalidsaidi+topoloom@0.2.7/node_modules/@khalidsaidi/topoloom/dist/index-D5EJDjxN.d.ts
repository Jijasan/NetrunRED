import { E as EdgeId, V as VertexId, G as Graph } from './index-TPgHfCmX.js';

type HalfEdgeId = number;
type FaceId = number;
type HalfEdgeRef = {
    edge: EdgeId;
    to: VertexId;
};
type RotationSystem = {
    order: HalfEdgeRef[][];
};
type HalfEdgeMesh = {
    halfEdgeCount: number;
    origin: VertexId[];
    twin: HalfEdgeId[];
    next: HalfEdgeId[];
    prev: HalfEdgeId[];
    edge: EdgeId[];
    face: FaceId[];
    faces: HalfEdgeId[][];
};
declare function rotationFromAdjacency(graph: Graph): RotationSystem;
declare function buildHalfEdgeMesh(graph: Graph, rotation: RotationSystem): HalfEdgeMesh;
declare function faces(mesh: HalfEdgeMesh): FaceId[];
declare function walkFace(mesh: HalfEdgeMesh, faceId: FaceId): HalfEdgeId[];
declare function validateMesh(mesh: HalfEdgeMesh): {
    ok: boolean;
    errors: string[];
};
declare function selectOuterFace(mesh: HalfEdgeMesh, positions?: Map<VertexId, {
    x: number;
    y: number;
}>): FaceId;

type index_FaceId = FaceId;
type index_HalfEdgeId = HalfEdgeId;
type index_HalfEdgeMesh = HalfEdgeMesh;
type index_HalfEdgeRef = HalfEdgeRef;
type index_RotationSystem = RotationSystem;
declare const index_buildHalfEdgeMesh: typeof buildHalfEdgeMesh;
declare const index_faces: typeof faces;
declare const index_rotationFromAdjacency: typeof rotationFromAdjacency;
declare const index_selectOuterFace: typeof selectOuterFace;
declare const index_validateMesh: typeof validateMesh;
declare const index_walkFace: typeof walkFace;
declare namespace index {
  export { type index_FaceId as FaceId, type index_HalfEdgeId as HalfEdgeId, type index_HalfEdgeMesh as HalfEdgeMesh, type index_HalfEdgeRef as HalfEdgeRef, type index_RotationSystem as RotationSystem, index_buildHalfEdgeMesh as buildHalfEdgeMesh, index_faces as faces, index_rotationFromAdjacency as rotationFromAdjacency, index_selectOuterFace as selectOuterFace, index_validateMesh as validateMesh, index_walkFace as walkFace };
}

export { type FaceId as F, type HalfEdgeMesh as H, type RotationSystem as R, type HalfEdgeId as a, type HalfEdgeRef as b, buildHalfEdgeMesh as c, faces as f, index as i, rotationFromAdjacency as r, selectOuterFace as s, validateMesh as v, walkFace as w };
