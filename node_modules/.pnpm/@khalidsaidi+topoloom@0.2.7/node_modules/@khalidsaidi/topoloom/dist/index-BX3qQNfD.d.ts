import { E as EdgeId, V as VertexId, G as Graph } from './index-TPgHfCmX.js';

type SCCResult = {
    components: VertexId[][];
    componentOf: number[];
};
declare function sccTarjan(graph: Graph): SCCResult;
type BiconnectedResult = {
    blocks: EdgeId[][];
    articulationPoints: VertexId[];
    bridges: EdgeId[];
    edgeToBlock: number[];
};
declare function biconnectedComponents(graph: Graph): BiconnectedResult;
type BCTreeNode = {
    id: number;
    type: 'block';
    edges: EdgeId[];
} | {
    id: number;
    type: 'cut';
    vertex: VertexId;
};
type BCTree = {
    nodes: BCTreeNode[];
    adj: number[][];
    blockNodes: number[];
    cutNodes: number[];
    edgeToBlock: number[];
};
declare function buildBCTree(graph: Graph, bcc?: BiconnectedResult): BCTree;

type index_BCTree = BCTree;
type index_BCTreeNode = BCTreeNode;
type index_BiconnectedResult = BiconnectedResult;
type index_SCCResult = SCCResult;
declare const index_biconnectedComponents: typeof biconnectedComponents;
declare const index_buildBCTree: typeof buildBCTree;
declare const index_sccTarjan: typeof sccTarjan;
declare namespace index {
  export { type index_BCTree as BCTree, type index_BCTreeNode as BCTreeNode, type index_BiconnectedResult as BiconnectedResult, type index_SCCResult as SCCResult, index_biconnectedComponents as biconnectedComponents, index_buildBCTree as buildBCTree, index_sccTarjan as sccTarjan };
}

export { type BCTree as B, type SCCResult as S, type BCTreeNode as a, type BiconnectedResult as b, biconnectedComponents as c, buildBCTree as d, index as i, sccTarjan as s };
